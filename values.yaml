# Default values for argocd-test-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: registry.hub.docker.com/ythihia/argocd-test
  pullPolicy: 
    - IfNotPresent
    - Always
    - Never
  tag:
    - v1
    - v2


